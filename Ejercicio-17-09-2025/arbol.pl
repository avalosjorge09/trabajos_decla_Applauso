arbol(
    caos,
    arbol(gea,
        arbol(cronos,
            arbol(hestia, null, null),
            arbol(demeter,
                null,
                arbol(zeus,
                    arbol(hera, null, null),
                    arbol(ares,
                        arbol(hefesto, null, null),
                        arbol(atenea,
                            arbol(dionisio,
                                arbol(apolo, null, null),
                                null
                            ),
                            arbol(afrodita, null, null)
                        )
                    )
                )
            )
        ),
        null
    ),
    arbol(uranos,
        arbol(rea,
            arbol(poseidon, null, null),
            arbol(ares, null, null)
        ),
        null
    )
).